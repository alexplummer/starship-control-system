
// Card
// ============
// Bordered wrapper for each ship

// Imports
import React from 'react';

class Card extends React.Component {

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div className="Card">
                <p>Card loaded!</p>
            </div>
        );
    }
}

// Exports
export default Card;
